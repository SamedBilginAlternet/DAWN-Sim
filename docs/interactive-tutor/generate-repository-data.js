/* Regenerates the offline source snapshot used by the tutor. Run with Node from repository root. */
const fs = require('fs');
const path = require('path');
const root = path.resolve(__dirname, '..', '..');
const files = [
  ['source/config.py','Config','Configuration'],['source/DawnSim.py','Core','Simulator infrastructure'],['source/DawnSimVis.py','Visualization','Visualization adapter'],
  ['topovis/common.py','Visualization','Drawing primitives'],['topovis/TopoVis.py','Visualization','Scene model'],['topovis/TkPlotter.py','Visualization','Tkinter renderer'],['topovis/__init__.py','Visualization','Package exports'],
  ['blank_template.py','Example','Starting template'],['flood.py','Example','Flooding protocol'],['flood_vis.py','Example','Visual flooding'],['aodv.py','Example','Simplified AODV'],['aodv_vis.py','Example','Visual simplified AODV'],
  ['README.md','Repository','Documentation'],['LICENSE','Repository','License'],['.gitignore','Repository','Repository hygiene']
];
const annotations = {
  'source/config.py:2':{title:'Broadcast uses an out-of-band address',tags:['Config','Beginner','Cross-file'],quick:'`BROADCAST_ADDR` is a module-level, constant-like Python name assigned the integer sentinel `-1`.',deep:'Node IDs are allocated from `0` upward by `Simulator.add_node()`, so `-1` cannot collide with a normal node ID. `BaseNode.send()` compares its `dest` argument with this value: equality means every currently in-range neighbor qualifies, not every node in the simulation.',why:'A sentinel keeps the public send API small: `send(-1, packet)` can represent broadcast without a separate broadcast method.',issue:'Python does not enforce constants. A `typing.Final` annotation or an enum would communicate intent more explicitly, but the current integer is cheap and works with the simple ID model.',related:['source/DawnSim.py:13','source/DawnSim.py:141','flood.py:33']},
  'source/DawnSim.py:19':{title:'Normal callbacks become SimPy-compatible generators',tags:['Core','Advanced','Generator'],quick:'`ensure_generator()` accepts either a generator function or an ordinary callback and returns a generator object in both cases.',deep:'SimPy process APIs expect an iterable generator. `inspect.isgeneratorfunction(func)` checks the function definition without executing it. If the callback is ordinary, the nested `_wrapper` calls it and then yields a zero-timeout, making the wrapper a generator function.',why:'Examples can write `run()` either with `yield` or as a plain function; the engine accepts both.',issue:'The wrapper deliberately schedules a zero-timeout after the callback. This is a compatibility adapter, not asynchronous work by itself.',related:['source/DawnSim.py:416','source/DawnSim.py:491']},
  'source/DawnSim.py:35':{title:'Euclidean distance powers radio reach and movement',tags:['Core','Math','Cross-file'],quick:'This helper computes straight-line distance between two `(x, y)` tuples using Pythagoras.',deep:'Tuple indexing obtains x and y. `** 2` squares each coordinate difference; `** 0.5` takes the square root. The result feeds range checks, sorted neighbor lists, propagation delay, and movement ratios.',why:'One shared helper keeps topology calculations consistent.',related:['source/DawnSim.py:148','source/DawnSim.py:235','source/DawnSim.py:473']},
  'source/DawnSim.py:141':{title:'The radio delivery loop begins here',tags:['Core','Runtime','Important'],quick:'`send(dest, pck)` iterates over the sending node’s distance-sorted neighbors and schedules delivery only for eligible receivers.',deep:'The node owns `neighbor_distance_list`; its `Simulator` maintains that list. The packet object is passed by reference to every scheduled receiver. A broadcast destination (`-1`) schedules all neighbors within `tx_range`; a numeric destination schedules only the matching in-range node.',why:'This models a minimal wireless radio abstraction while leaving routing logic in subclasses.',issue:'The same mutable dictionary is handed to every receiver. If one receiver mutates it before another callback runs, later receivers can observe the change. The simulator seed also does not control `random.random()` used for random delay.',related:['source/DawnSim.py:447','source/config.py:6','flood.py:33']},
  'source/DawnSim.py:164':{title:'A node owns the timers it creates',tags:['Core','Timer','State'],quick:'`set_timer()` constructs a `Timer`, saves its reference in `self.timers`, and returns it to the caller.',deep:'The list makes bulk cancellation possible through `kill_all_timers()`. Constructing `Timer` immediately calls `Timer.set()`, which registers its generator with the SimPy environment.',why:'Protocol subclasses can schedule delayed actions without touching SimPy directly.',issue:'Completed timers are not removed from `self.timers`; a long-running protocol that creates many timers can retain completed timer objects.',related:['source/DawnSim.py:322','source/DawnSim.py:181']},
  'source/DawnSim.py:231':{title:'One mobility tick updates position and topology',tags:['Core','Movement','Runtime'],quick:'`move_step()` advances toward `target_pos` by `SIM_MOVE_STEP_TIME × speed`, refreshes neighbors, and schedules another tick if needed.',deep:'It clamps the final step to avoid overshooting. Otherwise it calculates a ratio of desired step length to remaining distance and moves along the vector. Neighbor lists must be recalculated because `send()` relies on their sorted distances.',why:'Small scheduled increments make connectivity change during a simulation.',issue:'Calling `move()` again before a previous movement reaches its target can leave multiple scheduled `move_step()` callbacks active; the newest target is shared state, so those callbacks can interfere.',related:['source/config.py:8','source/DawnSim.py:250','source/DawnSim.py:447']},
  'source/DawnSim.py:322':{title:'Timer is a cancellable delayed callback',tags:['Core','Timer','SimPy'],quick:'`Timer` wraps a callback in a small SimPy process that waits, invokes the callback, or records cancellation.',deep:'`run()` yields `env.timeout(delay)`, suspending its generator until simulation time advances. `kill()` interrupts the process, making the `except simpy.Interrupt` path set `canceled`. `reset()` interrupts then creates a fresh process.',why:'It gives protocol code a familiar timer API without making every example manage process handles.',related:['source/DawnSim.py:164','source/DawnSim.py:337']},
  'source/DawnSim.py:371':{title:'Simulator owns the event clock and topology',tags:['Core','Architecture','Important'],quick:'`Simulator` creates the SimPy real-time environment, node collection, duration, per-instance random object, and timeout shortcut.',deep:'`simpy.rt.RealtimeEnvironment(factor=timescale, strict=False)` couples simulation advancement to wall-clock timing. `nodes` is the authoritative list; IDs are its indices. The `random.Random(seed)` instance exists, but the send path currently uses the module-level generator instead.',why:'It centralizes state shared by all nodes.',related:['source/DawnSim.py:385','source/DawnSim.py:431','source/DawnSim.py:483']},
  'source/DawnSim.py:447':{title:'Neighbor lists are kept sorted by distance',tags:['Core','Topology','Advanced'],quick:'This method removes stale entries involving one affected node, reinserts that node into every other node’s sorted list, then rebuilds the affected node’s own list.',deep:'`bisect.insort` inserts a `(distance, node)` tuple in sorted order. Ties can compare nodes, which is why `BaseNode.__lt__` compares IDs. Sorted lists let `send()` stop at the first distance beyond `tx_range` rather than scan farther nodes.',why:'Node additions and movement change only relationships involving the affected node, so this avoids rebuilding all lists from scratch.',related:['source/DawnSim.py:102','source/DawnSim.py:145','source/DawnSim.py:235']},
  'source/DawnSim.py:483':{title:'The simulation lifecycle is explicit and ordered',tags:['Core','Lifecycle','Important'],quick:'`run()` calls every node’s `init()`, registers every node’s `run()` as a SimPy process, advances until `duration`, then calls every `finish()`.',deep:'This gives protocol authors four hooks: initialize state, begin behavior, react to packets, and clean up. `ensure_generator` permits `run()` to be ordinary or generator-based.',why:'The ordering makes sample algorithms predictable and easy to teach.',related:['blank_template.py:11','flood.py:16','source/DawnSim.py:19']},
  'source/DawnSimVis.py:119':{title:'The visual simulator layers rendering over the same engine',tags:['Visualization','Architecture','Threading'],quick:'`DawnSimVis.Simulator` inherits the core `DawnSim.Simulator` and adds a Scene plus Tkinter Plotter only when `visual` is true.',deep:'The inherited simulator remains the source of simulation state. The visual adapter mirrors node/link operations into `topovis.Scene`, which informs `TkPlotter` and ultimately Tkinter Canvas.',why:'Examples can choose visual behavior without duplicating the event engine.',issue:'`run()` starts the simulation in a background thread while rendering uses the main Tk loop. Simulation-triggered scene updates can reach Tk canvas methods from that background thread; Tkinter thread safety is therefore an architectural concern, not a guarantee of failure.',related:['source/DawnSimVis.py:197','topovis/TopoVis.py:85','topovis/TkPlotter.py:17']},
  'topovis/TopoVis.py:56':{title:'A decorator forwards scene changes to renderers',tags:['Visualization','Decorator','Legacy'],quick:'`informPlotters` wraps Scene commands so each registered plotter receives a matching method call after the Scene updates its own model.',deep:'The wrapper has to preserve a callable signature because it forwards arbitrary arguments. This module uses legacy inspection helpers in its implementation; on modern Python, `inspect.signature()` and `functools.wraps()` are safer replacements.',why:'Scene methods update state once and broadcast the rendering command to every attached plotter.',issue:'The legacy `inspect.getargspec` / `inspect.formatargspec` APIs used by this decorator were removed in Python 3.11, so this source is not portable to current Python without modernization.',related:['topovis/TopoVis.py:134','topovis/TkPlotter.py:114']},
  'source/config.py:6':{title:'Delay mode is global module state',tags:['Config','Runtime','Issue'],quick:'The string `prop` chooses propagation delay in `BaseNode.send()`; `random` chooses a random delay; every other string silently uses the constant-delay branch.',deep:'The misspelled `SIM_MESSAGGING_*` identifier is part of the existing API and must be used exactly as written. Because config is imported as a module, changing it affects every simulator in the Python process.',why:'A small teaching simulator can expose configuration as simple constants.',issue:'Invalid values fall through to constant delay instead of raising an error. Per-simulator configuration would make concurrent simulations safer.',related:['source/DawnSim.py:150','source/config.py:7']},
  'README.md:27':{title:'Documentation diverges from the checked-in API',tags:['Repository','Documentation','Issue'],quick:'The README says `from source import DownSim`, but the repository contains `DawnSim.py` and examples import `from source import DawnSim`.',deep:'The following snippets repeat the typo, omit `self` in `def run()`, use malformed dictionary syntax, and instantiate `DawnSim` rather than `DawnSim.Simulator`.',why:'This line is documentation, not executable code; the tutor preserves it so discrepancies remain visible.',issue:'Treat the example scripts as the authoritative runnable usage until the README is corrected.',related:['flood.py:4','source/DawnSim.py:371']}
};
const data = files.map(([file,kind,group]) => ({file,kind,group,lines:fs.readFileSync(path.join(root,file),'utf8').replace(/\r\n/g,'\n').split('\n').slice(0,-1),annotations}));
fs.writeFileSync(path.join(__dirname,'repository-data.js'),`window.DAWN_TUTOR_DATA = ${JSON.stringify({files:data,annotations},null,2)};\n`);
console.log(`Generated ${data.length} source snapshots.`);
